[package]
name = "audio-splitter-gui"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.102"

[target.'cfg(target_os = "windows")'.dependencies]
eframe = { version = "0.33.3", default-features = false, features = ["default_fonts", "glow"] }
wasapi = "0.22.0"
